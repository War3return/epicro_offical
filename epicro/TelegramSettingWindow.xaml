<Window x:Class="epicro.TelegramSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="텔레그램 설정" Height="140" Width="300"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label x:Name="lbl_UserCount" Content="등록된 사용자: 0명" Grid.Row="0"
               FontSize="13" Padding="0,0,0,4"/>

        <Label Content="봇에게 /start 를 보내면 자동으로 등록됩니다." Grid.Row="1"
               Foreground="Gray" FontSize="10" Padding="0,0,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnTest" Content="테스트 전송" Width="80" Margin="0,0,5,0" Click="btnTest_Click"/>
            <Button Content="닫기" Width="55" Click="btnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
